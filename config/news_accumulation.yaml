news_accumulation:
  # N1.7: compared against THIS RUN's new items (news_source_runs), not the
  # whole accumulated store -- the old comparison could never fire.
  min_items_per_run: 1
  min_source_count: 1
  output_dir: outputs
  quality_status_thresholds:
    min_success_rate: 0.90
